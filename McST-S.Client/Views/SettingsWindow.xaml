<Window x:Class="McST_S.Client.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="设置" 
        Height="250" 
        Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="服务端设置" 
                   FontSize="16" 
                   FontWeight="Bold" 
                   Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" 
                    Orientation="Vertical" 
                    Margin="0,0,0,10">
            <TextBlock Text="服务端地址:" 
                       Margin="0,0,0,5"/>
            <TextBox x:Name="ServerUrlTextBox" 
                     Text="{Binding Config.ServerUrl}"/>
        </StackPanel>

        <StackPanel Grid.Row="2" 
                    Orientation="Vertical" 
                    Margin="0,0,0,20">
            <TextBlock Text="当前版本号:" 
                       Margin="0,0,0,5"/>
            <TextBox x:Name="VersionTextBox" 
                     Text="{Binding Config.CurrentVersion}"
                     IsReadOnly="True"
                     Foreground="Gray"
                     ToolTip="版本号由程序自动管理"/>
        </StackPanel>

        <TextBlock Grid.Row="3" 
                   Text="注意: 版本号由程序自动管理，无法手动修改。" 
                   FontStyle="Italic"
                   TextWrapping="Wrap"
                   Margin="0,0,0,20"/>

        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="取消" 
                    Width="80" 
                    Margin="0,0,10,0" 
                    Click="CancelButton_Click"/>
            <Button Content="保存" 
                    Width="80" 
                    Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window>